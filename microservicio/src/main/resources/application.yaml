jasypt:
   encryptor:
      password: root
      algorithm: PBEWithMD5AndDES

server:
   port: 8083
   servlet:
      context-path: /microservicio
      session:
         timeout: 21600s

spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      type: com.zaxxer.hikari.HikariDataSource
      jdbcUrl: jdbc:mysql://localhost:3306/eventos
      username: root
      password: 12345
      hikari:
         poolName: pool
